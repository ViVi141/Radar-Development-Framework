# EM体素场网络同步

## EMFieldNetworkAPI

网络集成的基础接口。默认为空实现。

---

## EMFieldNetworkComponent

服务器权威实现：

- **服务器**：执行帧衰减，通过不可靠RPC广播检测结果
- **客户端**：接收并存储结果；客户端→服务器RPC用于注册区域
- `[RplProp]` 用于同步雷达原点和状态

---

## EMDetectionResult

可序列化的检测结果（逗号分隔格式），用于RPC载荷。