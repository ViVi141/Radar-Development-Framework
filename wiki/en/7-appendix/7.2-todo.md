# TODO

Status and future ideas. See `TODO.md` in the repository for full details.

---

## Radar Module

**Completed**: EM model, RCS, Doppler, modes, HUD, presets, SAR, ECM, classification, tests.

**Future ideas**:
- PPI scan animation (rotating sweep line)
- Target trail history (retain N frames, fade)
- Hover/click target details (mouse events)
- A-Scope in HUD
- Multipath effects
- Antenna pattern weighting
- Network sync for radar scans

---

## LiDAR Module

**Completed**: Point cloud core, sampling strategies, visualization, CSV export, network sync, Bootstrap, cycler.

---

## EM Voxel Field

**Completed**: Two-level grid, injection/query API, passive sensor, network sync, debug viz.

**Future ideas**: Descriptor map updates, multi-field instances, persistence, GPU/parallel decay.
